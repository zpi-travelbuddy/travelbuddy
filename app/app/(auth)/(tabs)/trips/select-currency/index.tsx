import { StyleSheet } from "react-native";
import React from "react";
import SelectCurrencyView from "@/views/trips/SelectCurrencyView";

const index = () => {
  return <SelectCurrencyView />;
};

export default index;

const styles = StyleSheet.create({});
